#docker-compose.green.yml

#green
services:
  backend:
    build: .
    ports:
      - "8082:8080"
    container_name: spring-green
    environment:
      - TZ=Asia/Seoul
  redis:
    image: redis
    ports:
      - "6381:6379"
